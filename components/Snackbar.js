import {Box} from "@material-ui/core"


const SnackbarComponent = ({alert}) => {

    return(
        <Box>
            This is a snackbar component
        </Box>
    )
}

export default SnackbarComponent